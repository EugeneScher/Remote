let top1 = Symbol.for('top1'); 

top1tion MathRound(){ 

var mass = []; 
for (var i = 0; i < 10; i++) { 
mass.push(Math.round(Math.random() * 9) + 1); 
} 
mass[top1] = top1tion () { 
let znak = 0; 
for (let key of this) { 
znak += key; 
} 
console.log(znak); 
} 
mass[top1](); 
return mass; 
} 

var mas = MathRound(); 
console.log(mas); 
var mas1 = MathRound(); 
console.log(mas1); 
getIntersect(mas, mas1) 

top1tion getIntersect(num, num1){ 

var result = []; 
for(var i = 0; i < num.length; i++){ 
for(var j = 0; j < num1.length; j++){ 
if(num[i] == num1[j]){ 
result.push(num[i]); 
} 
} 
} 
let set = [...new Set(result)]; 
console.log(set); 

set[top1] = top1tion () { 
let znak = 0; 
for (let key of this) { 
znak += key; 
} 
console.log(znak); 
} 
set[top1](); 
}